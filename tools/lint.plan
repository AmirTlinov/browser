# Browser MCP — lint.plan

( [ -x .venv/bin/ruff ] && .venv/bin/ruff check . ) || ( python3 -m pip show ruff >/dev/null 2>&1 && python3 -m ruff check . ) || ruff check .
( [ -x .venv/bin/ruff ] && .venv/bin/ruff format --check . ) || ( python3 -m pip show ruff >/dev/null 2>&1 && python3 -m ruff format --check . ) || ruff format --check .
